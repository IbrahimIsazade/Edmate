using Application.Services;
using Domain.Entities;
using MediatR;
using Repositories;

namespace Application.Modules.StudentModule.Commands.AddCommand
{
    //internal class StudentAddCommandRequestHandler(IAwardRepository awardRepository, IEntityService entityService) : IRequestHandler<StudentAddCommandRequest, Student>
    //{
    //    public async Task<Student> Handle(StudentAddCommandRequest request, CancellationToken cancellationToken)
    //    {
    //        return await entityService.AddAsync(request, awardRepository, cancellationToken);
    //    }
    //}
}